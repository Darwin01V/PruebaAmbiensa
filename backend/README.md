Pasos para levantar el proyecto
- Ejecutra el comando composer install para instalar las dependencias 
- Configura la base de datos en el archivo .env toma de ejemplo el archivo .env.example
- Levanta el servidor con el comando php artisan serve
